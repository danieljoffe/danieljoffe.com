/** Full-width section with muted background — used for hero and report sections. */
export const sectionContainer =
  'w-full bg-surface-secondary overflow-hidden flex flex-col justify-center';

/** Constrained inner container for section content. */
export const sectionInner =
  'max-w-3xl mx-auto w-full px-4 sm:px-6 py-20 md:py-32';

/** Standard card surface with rounded corners and border. */
export const cardBase = 'bg-surface-secondary rounded-xl border border-border';

/** Consistent focus ring for interactive non-Button elements. */
export const focusRing =
  'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-500 focus-visible:ring-offset-2 focus-visible:ring-offset-surface';
