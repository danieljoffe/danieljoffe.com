# @danieljoffe.com/shared-ui

Shared React component library for the danieljoffe.com monorepo.

## Running unit tests

Run `pnpm nx test @danieljoffe.com/shared-ui` to execute the unit tests via [Jest](https://jestjs.io).
