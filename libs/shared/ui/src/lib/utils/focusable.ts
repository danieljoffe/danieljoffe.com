/** Selector for elements that can receive keyboard focus (Modal trap, Popover initial focus). */
export const FOCUSABLE_SELECTOR =
  'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])';
