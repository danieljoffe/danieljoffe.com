name: Size Limit

on:
  pull_request:
    branches: [develop]

permissions:
  pull-requests: write

concurrency:
  group: size-limit-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  size:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - uses: andresz1/size-limit-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          skip_step: build
          build_script: 'size:noop'
        # Note: The first PR adding size-limit will fail the base branch
        # comparison because develop doesn't have the size:noop script yet.
        # After this PR merges, all subsequent PRs will work correctly.
        continue-on-error: true

      # Fallback: run size-limit directly if the action comparison fails
      - name: Size check (standalone)
        run: pnpm run size
