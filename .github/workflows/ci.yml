name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: NX autofix=lint,tests
        run: npx nx-cloud start-ci-run --fix-tasks="*test*" --auto-apply-fixes="lint"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Lint
        run: yarn lint

      - name: Type check
        run: yarn nx run-many --target=typecheck --all

      - name: Unit tests
        run: yarn test:coverage

      - name: Build Storybook
        run: yarn nx build-storybook @danieljoffe.com/ui

      - name: Build application
        run: yarn nx build root --prod

      - name: Install Playwright browsers
        run: yarn playwright install --with-deps

      - name: Run E2E tests
        run: yarn nx run @danieljoffe.com/root-e2e:e2e
        env:
          CI: true
          # Disable Sentry and other services that might slow down CI
          SENTRY_DSN: ''
          SENTRY_AUTH_TOKEN: ''
          NEXT_TELEMETRY_DISABLED: '1'
          ANALYZE: 'false'

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: apps/root-e2e/playwright-report-json/
          retention-days: 30

      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-screenshots
          path: apps/root-e2e/test-results/
          retention-days: 30

      - name: Nx self healing CI
        run: npx nx fix-ci
        if: always()
